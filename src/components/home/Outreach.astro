---
/**
 * Outreach — deep band, one screen. Headline, the blog posts as a ruled
 * index, and a closing ask to be invited somewhere.
 *
 * Every row is a plain link to the post's own page (`/outreach/<id>/`),
 * where the post gets a real reading column, and the shared view
 * transition carries the reader over. The in-place popup this section used
 * to run is gone: a modal that had to be a whole page was a page, and the
 * page does it properly for crawlers, no-JS visitors and share links too.
 *
 * The index is the only ruled thing in the band: a hairline over it, one
 * under each entry, and three columns that repeat exactly down the list.
 */
import { getCollection } from 'astro:content';
import Reveal from '../Reveal.astro';

const base = import.meta.env.BASE_URL.replace(/\/+$/, '');
const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const shortDate = (d: Date) =>
  d.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' });
---

<section id="outreach" class="band-deep">
  <div class="outreach-frame mx-auto w-full max-w-[88rem] px-5 sm:px-8">
    <h2 class="type-display text-[clamp(2.5rem,7vw,6.5rem)]" data-split>Outreach</h2>

    <!-- POSTS: a ruled index. One entry per post, columns aligned across
         every row, so a season with one post reads the same as a season
         with ten. -->
    <div class="posts" data-reveal-group>
      {
        posts.map((post) => (
          <Reveal variant="up">
            <a class="post-row" href={`${base}/outreach/${post.id}/`}>
              <span class="post-date type-label">{shortDate(post.data.date)}</span>
              <span class="post-title type-display">{post.data.title}</span>
              <span class="post-desc">{post.data.description}</span>
            </a>
          </Reveal>
        ))
      }
      {posts.length === 0 && <p class="post-empty text-muted">No posts yet.</p>}
    </div>

    <!-- THE ASK: we want to be at their event, not the other way round. The
         button finishes the sentence rather than sitting under it, so the two
         share one baseline and one line (see .ask). -->
    <div class="ask">
      <h3 class="type-display text-[clamp(2rem,4.5vw,4.5rem)]" data-split>
        Got an event? We want in.
      </h3>
      <Reveal variant="up" delay={90}>
        <a href="#contact" class="btn btn-solid">Invite us</a>
      </Reveal>
    </div>
  </div>
</section>

<style>
  /* ---- The frame -------------------------------------------------- */
  /* One screen, three blocks, one gap between them: the headline, the index
     and the ask, spaced off a single measure instead of off three stacked
     paddings that happened to add up. */
  .outreach-frame {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: clamp(2.5rem, 8svh, 5.5rem);
    min-height: 100svh;
    padding-top: clamp(4rem, 11svh, 8rem);
    padding-bottom: clamp(4rem, 11svh, 8rem);
  }

  /* ---- Post index -------------------------------------------------- */
  /* An index, which is what a short list of posts is: ruled top and bottom,
     one rule between entries, and three columns set once on the row so the
     dates stack, the titles stack and the lines about them stack. First
     baselines level across all three, so the date reads as the title's
     dateline rather than as a block beside it. */
  .posts {
    border-top: 1px solid var(--color-line);
  }

  .post-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 0.55rem 0;
    padding: clamp(1.35rem, 3.2svh, 2.25rem) 0;
    border-bottom: 1px solid var(--color-line);
    text-decoration: none;
    color: var(--color-ink);
    transition: border-color 220ms ease;
  }

  .post-date {
    color: var(--color-faint);
    transition: color 200ms ease;
  }

  .post-title {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    line-height: 1.1;
    transition: color 200ms ease;
  }

  .post-desc {
    max-width: 52ch;
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--color-muted);
  }

  .post-empty {
    padding: 1.5rem 0;
    border-bottom: 1px solid var(--color-line);
  }

  @media (min-width: 64rem) {
    .post-row {
      grid-template-columns: 9rem minmax(0, 1.2fr) minmax(0, 1fr);
      gap: 0 clamp(2rem, 4vw, 4rem);
      align-items: baseline;
    }

    .post-desc {
      font-size: 1rem;
    }
  }

  /* Nothing moves: the title turns grape and the rule under the entry turns
     with it, so the whole row lights up without the list shifting under the
     pointer. Keyboard gets the same colour on every machine and for every
     motion preference; the ring says where you are, the colour says what it
     is. */
  .post-row:focus-visible .post-title {
    color: var(--color-accent);
  }

  @media (hover: hover) and (pointer: fine) {
    .post-row:hover {
      border-bottom-color: var(--color-accent);
    }

    .post-row:hover .post-title {
      color: var(--color-accent);
    }

    .post-row:hover .post-date {
      color: var(--color-ink);
    }
  }

  /* ---- The ask ---------------------------------------------------- */
  /* The button finishes the headline instead of sitting under it: one row,
     one baseline, the control trailing the last word with a gap that opens
     as the type grows. Where the line cannot hold both, the button takes the
     next line at the same left margin and the row gap stands in for the
     margin it used to carry.

     The headline caps at 4.5rem rather than the 5rem it ran at alone, which
     is what leaves room for the button on a laptop line; it still wraps and
     balances on its own below that.

     Both children are placed by this rule alone. The reveal wrapper comes
     out of Reveal.astro, so it never carries this component's scope and
     cannot be selected by class from here; flex defaults do the rest.

     Centred, not baselined: on the text's own baseline the button's box
     hung below the sentence, and a control that sags off the line it
     finishes reads as misaligned. Centring sets the block on the line's
     own band instead. */
  .ask {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    column-gap: clamp(1.5rem, 3vw, 2.75rem);
    row-gap: 2.5rem;
  }
</style>
